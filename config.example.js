// Copy this file to `config.local.js` (gitignored) and fill in your
// localhost-restricted DEV key. config.local.js is loaded after config.js
// and overrides it — so dev machines use the dev key, the published site
// falls back to the committed prod key in config.js.
window.APP_CONFIG_LOCAL = {
  mapyKey: 'YOUR_MAPY_DEV_KEY_HERE',
};
